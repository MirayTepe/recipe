import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 5,
    backgroundColor: '#fff',
    margin: 10,
  },
  text: {
    fontSize: 18,
    marginLeft: 10,
    color: '#000',
  },
});

export default styles;
